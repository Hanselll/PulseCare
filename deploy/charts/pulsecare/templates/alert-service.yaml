apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.services.alert.name }}
spec:
  replicas: {{ .Values.services.alert.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.services.alert.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.services.alert.name }}
    spec:
      initContainers:
        - name: wait-for-postgres
          image: {{ .Values.global.initContainerImage }}
          command: ["sh", "-c", "until nc -z postgres 5432; do echo waiting for postgres; sleep 2; done"]
        - name: wait-for-redis
          image: {{ .Values.global.initContainerImage }}
          command: ["sh", "-c", "until nc -z redis 6379; do echo waiting for redis; sleep 2; done"]
      containers:
        - name: {{ .Values.services.alert.name }}
          image: {{ include "pulsecare.image" (dict "root" . "repository" .Values.services.alert.repository) }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: 8000
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: pulsecare-db-secret
                  key: DATABASE_URL
            - name: REDIS_URL
              valueFrom:
                configMapKeyRef:
                  name: pulsecare-app-config
                  key: REDIS_URL
            - name: REDIS_LATENCY_MS
              valueFrom:
                configMapKeyRef:
                  name: pulsecare-app-config
                  key: REDIS_LATENCY_MS
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.alert.name }}
  labels:
    app: {{ .Values.services.alert.name }}
spec:
  selector:
    app: {{ .Values.services.alert.name }}
  ports:
    - name: http
      port: 8000
      targetPort: 8000
