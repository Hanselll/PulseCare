# PulseCare 被测系统开发需求文档

> 本文档仅描述 PulseCare 被测系统的产品、架构、功能、部署、可观测性与故障注入需求，不包含任何求职、简历或面试包装内容。

---

## 1. 项目概述

### 1.1 项目名称

**PulseCare：远程设备健康监测与告警系统**

### 1.2 项目定位

PulseCare 是一个模拟 IoT / 边缘设备健康监测场景的分布式微服务系统。系统持续接收设备状态数据，完成数据接入、数据校验、风险评分、告警生成、通知发送和状态查询。

PulseCare 的主要用途是作为云原生故障诊断系统的被测对象。它需要能够部署在 Kubernetes / AWS EKS 上，并产生足够真实的指标、日志、链路和 Kubernetes 事件，用于验证外部诊断系统对分布式故障的识别、分析和报告能力。

### 1.3 项目目标

PulseCare 需要满足以下目标：

1. 提供一个业务逻辑简单但链路完整的分布式系统。
2. 支持本地 Docker Compose 运行。
3. 支持 Kubernetes / AWS EKS 部署。
4. 每个服务具备健康检查、指标暴露和结构化日志。
5. 支持典型云原生故障场景注入。
6. 支持 Prometheus 指标采集。
7. 支持 OpenTelemetry 分布式链路追踪。
8. 支持 Redis、PostgreSQL 等真实分布式依赖。
9. 为外部诊断系统提供服务拓扑、SLO 和故障场景元数据。

---

## 2. 总体业务流程

### 2.1 正常业务链路

```text
device-simulator
    ↓
ingestion-service
    ↓
validation-service
    ↓
risk-scoring-service
    ↓
alert-service
    ↓
notification-service
```

辅助查询入口：

```text
dashboard-api
```

### 2.2 业务说明

1. `device-simulator` 定期生成模拟设备健康数据。
2. `ingestion-service` 接收设备上报数据。
3. `validation-service` 校验数据合法性。
4. `risk-scoring-service` 计算设备健康分和风险等级。
5. `alert-service` 根据风险等级生成告警，并进行去重和聚合。
6. `notification-service` 模拟发送通知。
7. `dashboard-api` 提供设备状态、告警和区域汇总查询。

---

## 3. 技术栈要求

### 3.1 编程语言

- Python 3.11+

### 3.2 Web 框架

- FastAPI
- Uvicorn
- Pydantic v2

### 3.3 服务间通信

阶段性演进：

1. 第一阶段：HTTP REST。
2. 第二阶段：Redis Stream 或 NATS。
3. 第三阶段：异步消费、重试机制、死信队列。

### 3.4 数据库

- PostgreSQL
- SQLAlchemy 2.x
- Alembic
- asyncpg

### 3.5 缓存与队列

- Redis

Redis 用途：

1. 设备最近状态缓存。
2. 告警去重。
3. 简单队列或流式消息。
4. 模拟缓存延迟或缓存不可用故障。

### 3.6 可观测性

- Prometheus metrics
- JSON structured logging
- OpenTelemetry tracing
- Grafana dashboard

### 3.7 容器与部署

- Docker
- Docker Compose
- Kubernetes YAML
- Helm Chart
- AWS EKS compatible

### 3.8 测试与质量工具

- pytest
- pytest-asyncio
- httpx
- ruff
- mypy，可选
- testcontainers，可选

---

## 4. 服务拆分需求

## 4.1 device-simulator

### 4.1.1 职责

`device-simulator` 负责持续生成模拟设备健康数据，并发送到 `ingestion-service`。

### 4.1.2 配置项

通过环境变量或配置文件控制：

```text
DEVICE_COUNT
SEND_INTERVAL_MS
ERROR_MODE
TARGET_INGESTION_URL
SCENARIO_NAME
REGION_COUNT
```

### 4.1.3 数据格式

```json
{
  "device_id": "dev-001",
  "timestamp": "2026-05-18T15:00:00Z",
  "temperature": 78.5,
  "voltage": 3.2,
  "heartbeat": true,
  "error_code": "E_OVERHEAT",
  "region": "gz-edge-1",
  "firmware_version": "1.0.3"
}
```

### 4.1.4 模拟模式

必须支持以下模式：

```text
normal
```

正常数据。

```text
overheat
```

高温设备增多。

```text
low_voltage
```

低电压设备增多。

```text
heartbeat_loss
```

心跳丢失。

```text
invalid_schema
```

字段缺失或字段格式错误。

```text
traffic_spike
```

上报流量突增。

```text
batch_offline
```

批量设备离线。

### 4.1.5 指标

```text
pulsecare_simulator_events_total
pulsecare_simulator_send_errors_total
pulsecare_simulator_send_latency_seconds
```

---

## 4.2 ingestion-service

### 4.2.1 职责

`ingestion-service` 负责接收设备上报数据，记录结构化日志，生成或透传 trace 信息，并调用下游校验服务。

### 4.2.2 API

```text
POST /api/v1/signals
GET  /healthz
GET  /readyz
GET  /metrics
```

### 4.2.3 POST /api/v1/signals 请求体

```json
{
  "device_id": "dev-001",
  "timestamp": "2026-05-18T15:00:00Z",
  "temperature": 78.5,
  "voltage": 3.2,
  "heartbeat": true,
  "error_code": null,
  "region": "gz-edge-1",
  "firmware_version": "1.0.3"
}
```

### 4.2.4 正常行为

1. 接收设备数据。
2. 记录 `signal_received` 日志。
3. 调用 `validation-service`。
4. 将校验通过的数据继续传递到后续链路。
5. 返回处理结果。

### 4.2.5 异常行为

1. 请求体非法时返回 400。
2. 下游超时时返回 502 或 504。
3. 下游不可用时记录 error log。
4. 所有错误必须带 trace_id。

### 4.2.6 指标

```text
pulsecare_ingestion_requests_total
pulsecare_ingestion_request_latency_seconds
pulsecare_ingestion_downstream_errors_total
pulsecare_ingestion_validation_failures_total
```

---

## 4.3 validation-service

### 4.3.1 职责

`validation-service` 负责校验设备数据的字段完整性、类型合法性和业务范围合法性。

### 4.3.2 API

```text
POST /api/v1/validate
GET  /healthz
GET  /readyz
GET  /metrics
```

### 4.3.3 校验规则

1. `device_id` 不能为空。
2. `timestamp` 不能为空。
3. `timestamp` 不能晚于当前时间 5 分钟以上。
4. `temperature` 必须在 -40 到 150 之间。
5. `voltage` 必须在 0 到 5 之间。
6. `heartbeat` 必须是 bool。
7. `region` 不能为空。
8. `firmware_version` 可选。

### 4.3.4 正常输出

```json
{
  "valid": true,
  "normalized_signal": {
    "device_id": "dev-001",
    "timestamp": "2026-05-18T15:00:00Z",
    "temperature": 78.5,
    "voltage": 3.2,
    "heartbeat": true,
    "error_code": null,
    "region": "gz-edge-1",
    "firmware_version": "1.0.3"
  }
}
```

### 4.3.5 异常输出

```json
{
  "valid": false,
  "errors": [
    {
      "field": "temperature",
      "message": "temperature out of range"
    }
  ]
}
```

### 4.3.6 指标

```text
pulsecare_validation_requests_total
pulsecare_validation_failures_total
pulsecare_validation_error_by_field_total
pulsecare_validation_latency_seconds
```

### 4.3.7 配置化要求

`required_fields` 必须可通过 ConfigMap 或环境变量配置，用于模拟错误配置导致的校验回归。

---

## 4.4 risk-scoring-service

### 4.4.1 职责

`risk-scoring-service` 负责根据设备状态计算健康分和风险等级。

### 4.4.2 API

```text
POST /api/v1/score
GET  /healthz
GET  /readyz
GET  /metrics
```

### 4.4.3 输入

```json
{
  "device_id": "dev-001",
  "timestamp": "2026-05-18T15:00:00Z",
  "temperature": 85.0,
  "voltage": 2.8,
  "heartbeat": true,
  "error_code": "E_OVERHEAT",
  "region": "gz-edge-1",
  "firmware_version": "1.0.3"
}
```

### 4.4.4 输出

```json
{
  "device_id": "dev-001",
  "health_score": 42,
  "risk_level": "HIGH",
  "risk_reasons": [
    "temperature_above_threshold",
    "voltage_below_threshold",
    "device_error_code_present"
  ]
}
```

### 4.4.5 风险等级

```text
LOW       80-100
MEDIUM    60-79
HIGH      40-59
CRITICAL  0-39
```

### 4.4.6 初始评分规则

1. 基础分为 100。
2. `temperature > 80`，扣 25 分。
3. `temperature > 100`，扣 45 分。
4. `voltage < 3.0`，扣 20 分。
5. `heartbeat = false`，扣 40 分。
6. `error_code` 不为空，扣 20 分。
7. 同一设备连续异常，额外扣 10 分。
8. 最低分为 0，最高分为 100。

### 4.4.7 设计模式要求

必须使用以下设计模式：

1. Strategy Pattern：不同评分策略。
2. Chain of Responsibility：评分规则链。
3. Factory Pattern：根据配置创建评分器。
4. Repository Pattern：查询设备历史状态。

### 4.4.8 指标

```text
pulsecare_scoring_requests_total
pulsecare_scoring_latency_seconds
pulsecare_scoring_risk_level_total
pulsecare_scoring_rule_hits_total
pulsecare_scoring_redis_errors_total
```

### 4.4.9 故障开关

必须支持以下环境变量：

```text
MEMORY_LEAK_MODE=false
SCORING_DELAY_MS=0
REDIS_LATENCY_MS=0
```

---

## 4.5 alert-service

### 4.5.1 职责

`alert-service` 负责根据风险评分结果生成告警，并进行去重、聚合和抑制。

### 4.5.2 API

```text
POST /api/v1/alerts/evaluate
GET  /api/v1/alerts
GET  /healthz
GET  /readyz
GET  /metrics
```

### 4.5.3 告警规则

1. `risk_level = HIGH`，生成 warning 告警。
2. `risk_level = CRITICAL`，生成 critical 告警。
3. 同一设备 5 分钟内相同原因只生成一次告警。
4. 同一区域 10 台以上设备 CRITICAL，生成区域级告警。

### 4.5.4 告警数据结构

```json
{
  "alert_id": "alert-001",
  "device_id": "dev-001",
  "region": "gz-edge-1",
  "severity": "critical",
  "reason": "device_health_critical",
  "status": "OPEN",
  "created_at": "2026-05-18T15:00:00Z"
}
```

### 4.5.5 告警状态

```text
OPEN
ACKED
RESOLVED
SUPPRESSED
```

### 4.5.6 指标

```text
pulsecare_alerts_created_total
pulsecare_alerts_suppressed_total
pulsecare_alerts_deduplicated_total
pulsecare_alert_evaluation_latency_seconds
pulsecare_alert_redis_errors_total
```

### 4.5.7 Redis 使用要求

Redis 用于：

1. 告警去重。
2. 区域级告警聚合。
3. 最近告警状态缓存。

当 Redis 不可用时，服务不能直接崩溃，必须记录错误日志并降级处理。

---

## 4.6 notification-service

### 4.6.1 职责

`notification-service` 负责模拟发送通知。系统不需要真实调用短信、邮件或外部 Webhook，只需要记录日志、指标和发送结果。

### 4.6.2 API

```text
POST /api/v1/notifications/send
GET  /healthz
GET  /readyz
GET  /metrics
```

### 4.6.3 输入

```json
{
  "alert_id": "alert-001",
  "target": "ops-team",
  "channel": "webhook",
  "severity": "critical",
  "message": "Device dev-001 is critical"
}
```

### 4.6.4 失败模式

通过环境变量控制：

```text
NOTIFICATION_FAILURE_MODE=none
NOTIFICATION_FAILURE_MODE=random_500
NOTIFICATION_FAILURE_MODE=always_500
NOTIFICATION_FAILURE_MODE=slow_response
```

### 4.6.5 重试要求

1. 最大重试次数默认为 3。
2. 支持指数退避。
3. 每次重试都必须记录日志。
4. 失败后写入 dead-letter log。

### 4.6.6 指标

```text
pulsecare_notification_sent_total
pulsecare_notification_failed_total
pulsecare_notification_retry_total
pulsecare_notification_latency_seconds
```

---

## 4.7 dashboard-api

### 4.7.1 职责

`dashboard-api` 提供设备状态、告警状态和区域汇总查询接口。

### 4.7.2 API

```text
GET /api/v1/devices
GET /api/v1/devices/{device_id}
GET /api/v1/alerts
GET /api/v1/alerts/{alert_id}
GET /api/v1/regions/{region}/summary
GET /healthz
GET /readyz
GET /metrics
```

### 4.7.3 查询能力

1. 按设备查询最近状态。
2. 按区域查询风险分布。
3. 查询当前 OPEN 告警。
4. 查询最近 N 分钟告警趋势。

### 4.7.4 指标

```text
pulsecare_dashboard_requests_total
pulsecare_dashboard_latency_seconds
pulsecare_dashboard_db_errors_total
```

---

## 5. 数据模型需求

## 5.1 DeviceSignal

字段：

```text
id
device_id
timestamp
temperature
voltage
heartbeat
error_code
region
firmware_version
created_at
```

## 5.2 DeviceHealth

字段：

```text
id
device_id
health_score
risk_level
risk_reasons
updated_at
```

## 5.3 Alert

字段：

```text
id
alert_id
device_id
region
severity
reason
status
created_at
updated_at
resolved_at
```

## 5.4 NotificationRecord

字段：

```text
id
notification_id
alert_id
target
channel
status
retry_count
error_message
created_at
```

## 5.5 ServiceEvent

用于记录故障场景或服务事件。

字段：

```text
id
scenario_name
service_name
event_type
message
created_at
```

---

## 6. 可观测性需求

## 6.1 日志要求

所有服务必须输出 JSON 结构化日志。

### 6.1.1 标准日志字段

```json
{
  "timestamp": "2026-05-18T15:00:00Z",
  "level": "INFO",
  "service": "ingestion-service",
  "trace_id": "abc123",
  "span_id": "span456",
  "device_id": "dev-001",
  "region": "gz-edge-1",
  "event": "signal_received",
  "message": "device signal received",
  "latency_ms": 23
}
```

### 6.1.2 错误日志字段

错误日志必须包含：

```text
error_type
error_message
downstream_service
retry_count
trace_id
```

---

## 6.2 Metrics 要求

所有服务必须暴露：

```text
GET /metrics
```

### 6.2.1 通用指标

```text
http_requests_total
http_request_duration_seconds
http_requests_in_progress
service_errors_total
downstream_errors_total
```

### 6.2.2 业务指标

```text
device_signals_total
validation_failures_total
risk_level_total
alerts_created_total
notifications_sent_total
notification_retry_total
```

---

## 6.3 Trace 要求

使用 OpenTelemetry。

每一次设备上报必须形成完整 Trace：

```text
device-simulator
  -> ingestion-service
  -> validation-service
  -> risk-scoring-service
  -> alert-service
  -> notification-service
```

Trace 中必须包含：

```text
service.name
http.route
http.status_code
latency
trace_id
span_id
error
```

---

## 6.4 Kubernetes 事件要求

通过部署配置和故障注入，系统应能产生以下 Kubernetes 事件或状态：

```text
Pod restart
OOMKilled
CrashLoopBackOff
Readiness probe failed
Liveness probe failed
ConfigMap updated
Deployment rollout
CPU throttling
```

---

## 7. 故障场景需求

## 7.1 场景一：CPU Throttling

### 7.1.1 目标

模拟 `ingestion-service` 在流量升高时因 CPU limit 过低导致延迟升高。

### 7.1.2 注入方式

1. 降低 `ingestion-service` CPU limit 到 `100m`。
2. 将 `device-simulator` 切换到 `traffic_spike` 模式。

### 7.1.3 预期现象

```text
ingestion-service p95 latency 升高
HTTP 请求超时增加
CPU throttling 指标升高
Pod 未必重启
```

### 7.1.4 预期证据

```text
container_cpu_cfs_throttled_periods_total 上升
http_request_duration_seconds p95 上升
ingestion-service 日志出现 processing delayed 或 timeout
```

---

## 7.2 场景二：配置回归导致 validation 错误率升高

### 7.2.1 目标

模拟发布错误配置后，大量合法请求被 `validation-service` 拒绝。

### 7.2.2 注入方式

在 ConfigMap 中新增错误必填字段：

```text
required_fields = ["device_id", "timestamp", "temperature", "firmware_build"]
```

设备数据中不包含 `firmware_build`。

### 7.2.3 预期现象

```text
validation-service 400 错误率上升
risk-scoring-service 流量下降
日志大量 missing field firmware_build
问题时间点与 ConfigMap 更新一致
```

### 7.2.4 预期证据

```text
pulsecare_validation_failures_total 上升
日志包含 missing field firmware_build
ConfigMap update event
risk-scoring-service requests_total 下降
```

---

## 7.3 场景三：risk-scoring-service OOMKilled

### 7.3.1 目标

模拟评分服务内存泄漏导致 Pod 被 OOMKilled。

### 7.3.2 注入方式

开启环境变量：

```text
MEMORY_LEAK_MODE=true
```

`risk-scoring-service` 将历史数据错误地追加到全局 list。

### 7.3.3 预期现象

```text
内存持续上涨
Pod restart count 增加
Kubernetes Event 出现 OOMKilled
alert-service 流量下降
```

### 7.3.4 预期证据

```text
container_memory_working_set_bytes 上升
kube_pod_container_status_restarts_total 上升
Pod Last State 为 OOMKilled
risk-scoring-service 日志在重启前中断
```

---

## 7.4 场景四：Redis 延迟导致告警链路变慢

### 7.4.1 目标

模拟 `alert-service` 依赖 Redis 去重，但 Redis 延迟升高导致告警处理变慢。

### 7.4.2 注入方式

任选一种方式：

1. 使用 Chaos Mesh 对 Redis 注入网络延迟。
2. 在服务中配置 `REDIS_LATENCY_MS` 模拟 Redis 慢响应。

### 7.4.3 预期现象

```text
alert-service latency 升高
redis timeout 日志增加
告警处理堆积
notification-service 接收延迟
```

### 7.4.4 预期证据

```text
pulsecare_alert_evaluation_latency_seconds 上升
pulsecare_alert_redis_errors_total 上升
日志包含 redis timeout
notification-service 接收量延迟变化
```

---

## 7.5 场景五：notification-service 重试风暴

### 7.5.1 目标

模拟通知服务下游失败，导致无效重试放大流量。

### 7.5.2 注入方式

```text
NOTIFICATION_FAILURE_MODE=always_500
RETRY_MODE=aggressive
```

### 7.5.3 预期现象

```text
notification retry_count 暴增
CPU 上升
日志大量 retry
alert-service 调用 notification-service 超时
```

### 7.5.4 预期证据

```text
pulsecare_notification_retry_total 上升
pulsecare_notification_failed_total 上升
notification-service CPU 使用率上升
日志包含 retry attempt
```

---

## 8. 阶段化开发计划

# 阶段一：领域模型与核心规则

## 目标

先不做微服务拆分，完成核心领域模型、数据结构、评分规则和告警规则。

## 范围

```text
建立项目结构
定义领域模型
实现设备数据校验
实现风险评分
实现告警判断
实现单元测试
```

## 目录建议

```text
pulsecare/
├── libs/
│   └── pulsecare-core/
│       ├── pulsecare_core/
│       │   ├── models.py
│       │   ├── schemas.py
│       │   ├── validation.py
│       │   ├── scoring.py
│       │   ├── alerting.py
│       │   └── errors.py
│       └── pyproject.toml
├── tests/
│   ├── unit/
│   │   ├── test_validation.py
│   │   ├── test_scoring.py
│   │   └── test_alerting.py
├── pyproject.toml
└── README.md
```

## 必须完成

1. `DeviceSignal` 模型。
2. `RiskScore` 模型。
3. `Alert` 模型。
4. 数据校验函数。
5. 风险评分规则。
6. 告警生成规则。
7. pytest 单元测试。

## 验收标准

1. `pytest` 全部通过。
2. risk scoring 覆盖 LOW / MEDIUM / HIGH / CRITICAL。
3. validation 覆盖非法字段、字段缺失、范围错误。
4. alerting 覆盖告警生成、去重和严重等级。

---

# 阶段二：MVP 微服务拆分

## 目标

将核心逻辑拆成最小可运行的 4 个微服务。

## 服务范围

```text
device-simulator
ingestion-service
risk-scoring-service
alert-service
```

此阶段可以暂不实现 `validation-service` 和 `notification-service`。

## 技术要求

```text
FastAPI
httpx
Pydantic v2
Uvicorn
Docker
Docker Compose
```

## 服务链路

```text
device-simulator
    ↓
ingestion-service
    ↓
risk-scoring-service
    ↓
alert-service
```

## 必须完成

1. 每个服务都有 `/healthz`。
2. 每个服务都有 `/readyz`。
3. 每个服务都有 `/metrics`。
4. 每个服务输出 JSON 日志。
5. 每个服务支持 Dockerfile。
6. Docker Compose 能本地启动。

## 验收标准

运行：

```bash
docker compose up --build
```

预期：

```text
所有服务启动成功
device-simulator 持续发送数据
ingestion-service 接收请求
risk-scoring-service 输出风险等级
alert-service 生成告警日志
```

---

# 阶段三：接入 PostgreSQL 与 Redis

## 目标

让系统具备真实分布式依赖，方便制造数据库和缓存类故障。

## 新增组件

```text
PostgreSQL
Redis
```

## 数据持久化

1. DeviceSignal 写入 PostgreSQL。
2. RiskScore 写入 PostgreSQL。
3. Alert 写入 PostgreSQL。
4. 告警去重状态写入 Redis。
5. 设备最近状态写入 Redis。

## 技术要求

```text
SQLAlchemy 2.x
Alembic
asyncpg
redis-py asyncio
Repository Pattern
Unit of Work，可选
```

## 必须完成

1. 数据库 migration。
2. Repository 层。
3. Redis client adapter。
4. 数据库连接失败处理。
5. Redis 连接失败处理。

## 验收标准

1. PostgreSQL 中可以查询设备数据和告警数据。
2. Redis 中可以查询最近设备状态。
3. alert-service 可以基于 Redis 去重。
4. Redis 停止时，alert-service 记录错误日志但服务不崩溃。

---

# 阶段四：补齐 validation-service 与 notification-service

## 目标

补齐完整业务链路，让系统具备更丰富的诊断场景。

## 新增服务

```text
validation-service
notification-service
```

## 完整链路

```text
device-simulator
    ↓
ingestion-service
    ↓
validation-service
    ↓
risk-scoring-service
    ↓
alert-service
    ↓
notification-service
```

## validation-service 需求

1. 字段校验。
2. schema 配置化。
3. 支持 ConfigMap 注入 `required_fields`。
4. 输出 validation failure 指标。

## notification-service 需求

1. 模拟通知发送。
2. 支持失败模式。
3. 支持慢响应模式。
4. 支持重试。
5. 输出 retry 指标。

## 验收标准

1. 完整链路可以跑通。
2. 合法数据可以走完整流程。
3. 非法数据会被 validation-service 拒绝。
4. CRITICAL 风险会触发 notification-service。
5. notification-service 失败时会产生 retry 日志和指标。

---

# 阶段五：可观测性增强

## 目标

让被测系统具备完整的可观测性能力。

## 必须接入

```text
Prometheus metrics
OpenTelemetry trace
JSON structured logging
Grafana dashboard
```

## 指标要求

每个服务必须暴露：

1. 请求总数。
2. 请求延迟。
3. 错误数。
4. 下游调用错误数。
5. 业务处理数。

核心业务指标：

```text
device_signals_total
validation_failures_total
risk_level_total
alerts_created_total
notifications_sent_total
notification_retry_total
```

## Trace 要求

完整设备链路必须能形成一个 trace：

```text
device-simulator -> ingestion -> validation -> scoring -> alert -> notification
```

## 日志要求

所有日志必须包含：

```text
service
trace_id
device_id
region
event
latency_ms
error_type，可选
```

## 验收标准

1. Prometheus 能抓到所有服务指标。
2. Grafana 能看到服务延迟和错误率。
3. Trace 后端能看到完整调用链。
4. 日志中能用 trace_id 串联一次设备上报。

---

# 阶段六：Kubernetes 与 Helm 部署

## 目标

让 PulseCare 可以部署到本地 Kubernetes 和 AWS EKS。

## 需要提供

```text
Kubernetes YAML
Helm Chart
Namespace
Deployment
Service
ConfigMap
Secret
HPA，可选
Resource requests/limits
Readiness Probe
Liveness Probe
```

## Helm Chart 结构

```text
deploy/helm/pulsecare/
├── Chart.yaml
├── values.yaml
├── templates/
│   ├── ingestion-deployment.yaml
│   ├── validation-deployment.yaml
│   ├── scoring-deployment.yaml
│   ├── alert-deployment.yaml
│   ├── notification-deployment.yaml
│   ├── simulator-deployment.yaml
│   ├── services.yaml
│   ├── configmap.yaml
│   └── hpa.yaml
```

## 资源限制要求

每个服务都要有默认资源配置：

```yaml
resources:
  requests:
    cpu: "100m"
    memory: "128Mi"
  limits:
    cpu: "500m"
    memory: "512Mi"
```

部分故障场景需要能通过 values 文件覆盖资源限制。

## 验收标准

执行：

```bash
helm install pulsecare deploy/helm/pulsecare -n pulsecare --create-namespace
```

预期：

```text
所有 Pod Running
所有 Service 可访问
Prometheus 可抓取指标
device-simulator 正常产生流量
```

---

# 阶段七：故障注入场景开发

## 目标

让 PulseCare 能主动进入可复现的异常状态。

## 故障场景目录

```text
chaos-scenarios/
├── cpu-throttling/
│   ├── README.md
│   └── values-cpu-low.yaml
├── bad-validation-config/
│   ├── README.md
│   └── bad-config.yaml
├── scoring-oom/
│   ├── README.md
│   └── values-memory-leak.yaml
├── redis-latency/
│   ├── README.md
│   └── chaosmesh-network-delay.yaml
├── notification-retry-storm/
│   ├── README.md
│   └── values-retry-storm.yaml
```

## 每个场景 README 必须包含

1. 场景目标。
2. 注入方式。
3. 预期现象。
4. 预期指标变化。
5. 预期日志关键词。
6. 预期 Kubernetes Event。
7. 恢复方法。

## 验收标准

1. 每个故障都能稳定复现。
2. 每个故障都有可观察证据。
3. 每个故障都能恢复到正常状态。

---

# 阶段八：诊断系统联动元数据

## 目标

为外部诊断系统提供服务拓扑、SLO 和故障提示元数据。

## 新增文件

```text
docs/diagnosis-integration.md
docs/service-topology.yaml
docs/slo.yaml
docs/diagnosis-hints.yaml
```

## service-topology.yaml 示例

```yaml
services:
  - name: ingestion-service
    downstream:
      - validation-service
  - name: validation-service
    downstream:
      - risk-scoring-service
  - name: risk-scoring-service
    downstream:
      - alert-service
  - name: alert-service
    downstream:
      - notification-service
```

## slo.yaml 示例

```yaml
slos:
  ingestion-service:
    availability: 99.9
    p95_latency_ms: 300
    error_rate_threshold: 0.01
  validation-service:
    availability: 99.9
    p95_latency_ms: 200
    error_rate_threshold: 0.02
```

## diagnosis-hints.yaml 示例

```yaml
hints:
  cpu_throttling:
    metrics:
      - container_cpu_cfs_throttled_periods_total
      - http_request_duration_seconds
    logs:
      - "request timeout"
      - "processing delayed"
    k8s_events:
      - "CPU throttling"
```

## 验收标准

1. 外部系统可以读取服务拓扑。
2. 外部系统可以读取 SLO 配置。
3. 外部系统可以根据 hints 理解故障场景的关键证据。

---

# 阶段九：CI/CD 与工程质量

## 目标

建立基础工程质量闭环。

## GitHub Actions 要求

```text
lint
unit test
integration test
docker build
helm lint
k8s manifest validation
```

## 推荐 workflow

```text
.github/workflows/ci.yml
.github/workflows/docker-build.yml
.github/workflows/helm-check.yml
```

## 质量门禁

1. `ruff check` 必须通过。
2. `pytest` 必须通过。
3. Docker image 能成功构建。
4. Helm chart 能成功 lint。

## 验收标准

Pull Request 自动执行 CI，并输出测试结果。

---

# 阶段十：文档完善

## 目标

让项目可以被独立部署、运行、观测和故障复现。

## 必须文档

```text
README.md
docs/architecture.md
docs/local-run.md
docs/k8s-deploy.md
docs/eks-deploy.md
docs/observability.md
docs/failure-scenarios.md
docs/diagnosis-integration.md
```

## README 必须包含

1. 项目定位。
2. 系统架构图。
3. 服务列表。
4. 本地启动方式。
5. Kubernetes 部署方式。
6. 故障场景列表。
7. 可观测性说明。

---

## 9. 推荐最终目录结构

```text
pulsecare/
├── services/
│   ├── device-simulator/
│   │   ├── app/
│   │   ├── Dockerfile
│   │   └── pyproject.toml
│   ├── ingestion-service/
│   │   ├── app/
│   │   ├── Dockerfile
│   │   └── pyproject.toml
│   ├── validation-service/
│   │   ├── app/
│   │   ├── Dockerfile
│   │   └── pyproject.toml
│   ├── risk-scoring-service/
│   │   ├── app/
│   │   ├── Dockerfile
│   │   └── pyproject.toml
│   ├── alert-service/
│   │   ├── app/
│   │   ├── Dockerfile
│   │   └── pyproject.toml
│   ├── notification-service/
│   │   ├── app/
│   │   ├── Dockerfile
│   │   └── pyproject.toml
│   └── dashboard-api/
│       ├── app/
│       ├── Dockerfile
│       └── pyproject.toml
├── libs/
│   └── pulsecare-core/
│       ├── pulsecare_core/
│       │   ├── models.py
│       │   ├── schemas.py
│       │   ├── logging.py
│       │   ├── metrics.py
│       │   ├── tracing.py
│       │   ├── validation.py
│       │   ├── scoring.py
│       │   ├── alerting.py
│       │   └── errors.py
│       └── pyproject.toml
├── deploy/
│   ├── docker-compose.yml
│   └── helm/
│       └── pulsecare/
├── observability/
│   ├── prometheus/
│   ├── grafana/
│   └── otel-collector/
├── chaos-scenarios/
│   ├── cpu-throttling/
│   ├── bad-validation-config/
│   ├── scoring-oom/
│   ├── redis-latency/
│   └── notification-retry-storm/
├── docs/
│   ├── architecture.md
│   ├── local-run.md
│   ├── k8s-deploy.md
│   ├── eks-deploy.md
│   ├── observability.md
│   ├── failure-scenarios.md
│   └── diagnosis-integration.md
├── tests/
│   ├── unit/
│   ├── integration/
│   └── e2e/
├── .github/
│   └── workflows/
├── README.md
└── pyproject.toml
```

---

## 10. MVP 收敛建议

第一版不建议一次实现全部服务。建议先完成：

```text
device-simulator
ingestion-service
risk-scoring-service
alert-service
Redis
PostgreSQL
Prometheus
Grafana
Docker Compose
基础 Helm Chart
```

第一版故障场景只做：

```text
CPU throttling
risk-scoring-service OOMKilled
Redis 延迟导致 alert-service 变慢
```

第一版可观测性只做：

```text
Prometheus metrics
JSON logs
Kubernetes Events
```

OpenTelemetry Trace 可以在第二版补充。

---

## 11. 最终交付物清单

开发完成后，PulseCare 至少应该交付：

1. 可本地运行的 Docker Compose 环境。
2. 可部署 Kubernetes / AWS EKS 的 Helm Chart。
3. 4 到 7 个 Python 微服务。
4. PostgreSQL 与 Redis 依赖。
5. Prometheus 指标暴露。
6. JSON 结构化日志。
7. OpenTelemetry Trace。
8. 3 到 5 个故障注入场景。
9. 完整 README 和部署文档。
10. 服务拓扑、SLO、故障提示元数据。

---

## 12. 非目标范围

以下内容不属于 PulseCare 第一版目标：

1. 不实现真实短信、邮件或第三方通知发送。
2. 不实现复杂前端页面。
3. 不实现真实 IoT 设备协议，例如 MQTT、CoAP。
4. 不实现复杂权限系统。
5. 不实现多租户管理。
6. 不实现商业级计费或用户系统。
7. 不依赖任何私有业务数据。

---

## 13. 总结

PulseCare 的核心价值是提供一个可控、可观测、可故障注入的分布式被测系统。

它的设计原则是：

```text
业务逻辑简单
服务链路完整
依赖组件真实
故障场景可复现
指标日志足够丰富
Kubernetes 部署真实
外部诊断系统容易接入
```

PulseCare 不追求复杂业务，而追求成为一个高质量的云原生诊断靶场系统。
